{
  "name": "space-learn",
  "private": true,
  "description": "Orchestration only — web/ and api/ are the real projects. Run `npm run dev` from here to start both.",
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "db:list": "supabase migration list",
    "db:push": "supabase db push",
    "db:new": "supabase migration new",
    "db:diff": "supabase db diff"
  },
  "devDependencies": {
    "supabase": "^2.113.0"
  }
}
